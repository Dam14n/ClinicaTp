import { ExcludeFilterPipe } from './exclude-filter.pipe';

describe('ExcludeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ExcludeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
